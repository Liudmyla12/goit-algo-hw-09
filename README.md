# goit-algo-hw-09

Домашнє завдання до теми **“Жадібні алгоритми та динамічне програмування”**.

Мета: реалізувати два підходи до задачі видачі решти монетами **[50, 25, 10, 5, 2, 1]** і порівняти їх ефективність (практично через `timeit` та теоретично через оцінку складності).

---

## 📌 Структура репозиторію

```text
goit-algo-hw-09/
├── task_1.py      # find_coins_greedy + find_min_coins + приклади
├── task_2.py      # Порівняння швидкодії (timeit)
├── README.md
└── .gitignore
```

---

## ✅ Завдання

### **Task 1 — Greedy (жадібний алгоритм)**

Функція **`find_coins_greedy(amount)`**:

- приймає суму `amount`
- повертає словник `{номінал: кількість}`
- працює жадібно: спочатку бере найбільші монети

Приклад: для `113` → `{50: 2, 10: 1, 2: 1, 1: 1}`

---

### **Task 2 — Dynamic Programming (мінімальна кількість монет)**

Функція **`find_min_coins(amount)`**:

- знаходить розклад суми з **мінімальною кількістю монет** (DP підхід)
- повертає словник `{номінал: кількість}`

Приклад: для `113` → `{1: 1, 2: 1, 10: 1, 50: 2}`

---

## ▶️ Запуск (Windows)

### 1) Перевірка роботи функцій

```bash
py .\task_1.py
```

### 2) Порівняння швидкодії (timeit)

```bash
py .\task_2.py
```

Опції:

```bash
py .\task_2.py --amounts 113,1000,5000,10000,50000 --number 200 --repeat 5
```

---

## 📈 Порівняння ефективності

### Теоретично (O-нотація)

- **Greedy:** приблизно **O(k)**, де `k` — кількість номіналів (у нас `k = 6`).
  Дуже швидкий, бо проходить по списку монет один раз.
- **DP:** **O(amount · k)** за часом і **O(amount)** за пам’яттю.
  Для великих сум працює повільніше, бо рахує оптимум для всіх проміжних сум від `1` до `amount`.

### Практично (timeit) — мої результати запуску

> Налаштування: `number=200`, `repeat=5`, у таблиці наведено **найкращий (min)** час з повторів.

| Amount | Greedy (s) |   DP (s) | Faster |
| -----: | ---------: | -------: | :----: |
|    113 |   0.000126 | 0.011767 | Greedy |
|   1000 |   0.000108 | 0.127523 | Greedy |
|   5000 |   0.000075 | 0.590553 | Greedy |
|  10000 |   0.000077 | 1.123781 | Greedy |
|  50000 |   0.000071 | 6.814741 | Greedy |

Ключове спостереження: час **DP** росте приблизно лінійно від суми (бо **O(amount · k)**), тоді як **Greedy** майже не змінюється, бо **O(k)** і `k` у нас сталий.

---

## ✅ Висновки

1. **Greedy** — найшвидший для великих сум у цій задачі, бо його складність залежить тільки від кількості номіналів монет (**O(k)**).
2. **DP** — повільніший на великих сумах, зате гарантує **мінімальну кількість монет** для будь-яких наборів номіналів (**O(amount · k)**).
3. Для набору монет **[50, 25, 10, 5, 2, 1]** greedy практично дає оптимальний результат (це “канонічна” система), але загалом для інших наборів монет greedy **може** дати не-оптимум — тоді DP буде правильним вибором.

---

## 🧰 Вимоги

- Python 3.x
- PowerShell / Terminal
- VS Code (опційно)
